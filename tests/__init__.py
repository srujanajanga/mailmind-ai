"""Test package for MailMind AI.

Marks the ``tests`` directory as a package so shared helpers and fixtures in
:mod:`tests.conftest` are importable when the suite is run from the repository
root via ``pytest``.
"""

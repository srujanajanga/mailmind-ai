"""REST API package for MailMind AI.

Houses the FastAPI application that exposes the agent over HTTP. The framework
imports live in :mod:`mailmind.api.main`; importing this package alone is cheap
and never pulls in FastAPI.
"""
